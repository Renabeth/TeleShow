import React, { useState } from "react";

const [followedChoice, setfollowedChoice] = useState({});
const [followedChoiceModal, setFollowedChoiceModal] = useState(false);
const [followedMedia, setFollowedMedia] = useState({});
{
  /*
            A way to view followed media 
            <div>
              {isLoggedIn &&
                followedMedia.followed?.map((media) => (
                  <div key={`followed-${media.media_id}-${media.media_type}`} className="followed-media">
                    <img className="followed-posters"
                    src={imgPath + media.poster_path}
                    alt={media.title}
                    onClick={()=>handleFollowedClick(media)}
                    />
                    <p>{media.title}</p>
                  </div>
                ))}
            </div>
            */
}
